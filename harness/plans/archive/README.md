# Archived Plans

这里保存已经退出活跃区的计划。

## 已归档

- `harness-trim-down.md`：已完成。当前仓库已经采用角色边界、Git hook、自检门禁和 `harness/records/` 留档的精简 harness。
- `harness-runtime-enforcement.md`：已取代。该计划中的 session、evidence、flow、report、pre-stop 等重型生命周期方案已被 `harness-trim-down.md` 的精简方案替换。

## 规则

- 完成的计划从 `harness/plans/` 移入本目录。
- 被取代或放弃的计划也移入本目录，但必须标明状态，不能伪装为完成。
- 活跃计划区只保留仍准备执行或正在执行的计划。
