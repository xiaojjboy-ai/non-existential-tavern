# Project Status

这里放项目当前状态。它不是工作流水账，而是给下一个 agent 或成员快速接管用的事实面板。

目录：

- `overview/`：整体状态。
- `runtime/`：Web 运行时、状态机、构建验证。
- `content/`：剧情、设定、脚本格式。
- `harness/`：Agent Team Harness 自身状态。
- `risks/`：已知风险和待处理问题。

规则：

- 状态文件写事实，不写空泛愿望。
- 状态过期要改，不要让下一个人猜。
- 重大状态变化必须同步工作记录。

