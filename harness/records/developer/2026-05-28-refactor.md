# UI模块拔插化重构

**人类负责人**：xiaojjboy-ai (或用户)
**角色**：developer
**Agent 工具**：Antigravity
**接任务**：2026-05-28 — 放弃重度OOP，重构主干流程为纯数据驱动的组件拼装模式
**完成**：2026-05-28 — 成功将复杂的长主页分拆为独立可插拔的模块

## 改了什么

- 彻底移除了 `src/app/page.tsx` 中杂乱的代码，改为单纯的组件容器。
- 抽离出独立的 `DialogueBox.tsx` 负责对话和打字机逻辑。
- 抽离出独立的 `ChoiceMenu.tsx` 负责分支选项按钮的呈现与跳转。
- 抽离出独立的 `DrinkPrompt.tsx` 负责在调酒节点呈现引导。
- 抽离出独立的 `DebugPanel.tsx` 负责全局状态和数据的悬浮监控。
- 修复了因为早期 OOP 遗留下的状态类型隐式依赖问题。

## 怎么验证

- 运行 `npm run compile` 重新生成了 `plot-data.json`，无警告。
- 运行 `npx tsc --noEmit` 通过严格的类型安全校验。
- 运行 `npm run build` 成功完成 Next.js 的生产环境构建。
